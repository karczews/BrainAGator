{
  "name": "brainagator",
  "version": "1.0.0",
  "description": "Pure KMP app for younglings",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^20.0.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^9.0.0"
  },
  "scripts": {
    "prepare": "husky"
  }
}
